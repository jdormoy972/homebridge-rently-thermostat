{
    "name": "homebridge-rently-thermostat",
    "version": "1.0.0",
    "description": "homebridge-rently-thermostat",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
        "homebridge-plugin",
        "rently-thermostat"
    ],
    "author": "",
    "license": "ISC",
    "engines": {
        "homebridge": ">=0.2.5",
        "node": ">=7.0.0"
    },
    "dependencies": {
        "axios": "^1.6.8",
        "homebridge-lib": "^6.7.5"
    }
}
